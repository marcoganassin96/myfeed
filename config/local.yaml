aws:
  region: "eu-west-1"

api:
  url: "http://localhost:8000"

cognito:
  user_pool_id: "eu-west-1_EU46ArwA7"
  client_id: "526dv44rl48pk6bp2pcphi6r89"

database:
  host: "localhost"
  port: 5432
  local_port: 15432  # used only when BASTION_ID is set (SSM tunnel)
  name: "newsletter"
  user: "newsletter"

redis:
  host: "localhost"
  port: 6379
  local_port: 16379  # used only when BASTION_ID is set (SSM tunnel)
  ssl: false

mdg:
  url: "http://localhost:9000"
